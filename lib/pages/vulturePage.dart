import 'package:flutter/material.dart';
class VulturePage extends StatelessWidget {
  const VulturePage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset("images/vulture.png");
  }
}